// MediaProcessor — transcodificação via FFmpeg (server-side, dentro do worker).
// Gera versões compatíveis por plataforma sem degradar/alterar o arquivo
// original. Será implementado junto da ETAPA 2/7. Ver docs/ARCHITECTURE.md.

export {};
